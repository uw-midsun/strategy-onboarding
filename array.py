def find_min_index(arr):
    '''
    Find index where minimum value first occurs in array
    eg. [1, 33, 1, -2, 0] => 3
    eg. [0, 0, 0, 0, 0] => 0

    @param array of integers
    @returns integer index of minimum integer
    '''

    array = open(test_fibonacci.py)
    b=sorted(array)
    s=len(a)
    print(b[0])
    print(b[s-1])
    
    pass


def reverse_str_arr(string):
    '''
    Return array of string characters, but in reverse order
    eg. "abc" => ['c', 'b', 'a']
    eg. "abba" => ['a', 'b', 'b', 'a']
    eg. "!!-.2" => ['2', '.', '-', '!', '!']

    @param string
    @returns array of characters
    '''

    string = open(test_fibonacci.py) # initial string
    stringlength=len(string) # calculate length of the list
    slicedString=s[stringlength::-1] # slicing the string
    print (slicedString) # printing the reversed string
    pass
